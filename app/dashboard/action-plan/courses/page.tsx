import { GenericActionPlanPage } from "../../../../components/GenericActionPlanPage";

export default function CoursesPage() {
  return (
    <GenericActionPlanPage 
      collectionName="courses"
      pageTitle="Courses & Training"
      pageSubtitle="Manage available courses, training sessions, and upload module highlights."
    />
  );
}
